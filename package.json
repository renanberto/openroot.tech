{
  "name": "openroot-tech",
  "version": "0.2.4-content-engine",
  "private": true,
  "description": "OpenRoot OS with structured portfolio content engine for nonroot mode",
  "scripts": {
    "validate:js": "find js -name \"*.js\" -print0 | xargs -0 -n1 node --check",
    "validate:site": "node scripts/validate-site.js",
    "build:content": "node scripts/build-content.js",
    "predeploy": "npm run validate:js && npm run validate:site"
  },
  "devDependencies": {}
}
